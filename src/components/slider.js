import React from "react";
import {Box} from "@mui/material";

const Slider = () =>{
    return (
        <Box sx={{height: "40px", width: "100%", backgroundColor: "blue", display: "flex"}}></Box>
    );
}

export default Slider;